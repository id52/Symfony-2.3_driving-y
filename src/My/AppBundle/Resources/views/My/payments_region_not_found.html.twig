{% extends 'AppBundle::layout_new.html.twig' %}

{% block content %}
<nav class="path">
    <a href="{{ path('homepage') }}">{{ 'titles.homepage'|trans }}</a>
    » <strong>{{ 'titles.payments'|trans }}</strong>
</nav>

<div class="mainContent">
    <div class="title"><h1>{{ 'payments.region_not_found'|trans }}</h1></div>
    {{ 'payments.region_not_found_description'|trans|raw }}
    <div class="links">
        <a class="bg-green" href="{{ path('my_profile_edit') }}">{{ 'payments.set_region'|trans }}</a>
    </div>
</div>
{% endblock content %}
