{% extends 'AppBundle::layout_new.html.twig' %}

{% block content %}
    <div class="main_t">
        <h1>{{ 'test.complete'|trans }}</h1>
    </div>
    <div id="test">
        <div class="form_unswer">
            <div class="alert-success clear">
                {{ settings['training_test_complete']|raw }}
            </div>
            <br />
            {% include 'AppBundle:My:_stat.html.twig' %}
            <br />
            <input type="button" onclick="document.location.href = '{{ path('my_test') }}';" value="{{ 'test_return'|trans }}" class="pull_left btn">
        </div>
    </div>
{% endblock content %}
