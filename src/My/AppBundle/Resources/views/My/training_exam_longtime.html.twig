{% extends 'AppBundle::layout_new.html.twig' %}

{% block content %}
<nav class="path">
    <a href="{{ path('homepage') }}">{{ 'titles.homepage'|trans }}</a>
    » <a href="{{ path('my_trainings') }}">{{ 'titles.trainings'|trans }}</a>
    » <a href="{{ path('my_training', { id: subject.id }) }}">{{ subject.title }}</a>
    » <strong>{{ 'exam_title'|trans({}, 'exam') }}</strong>
</nav>

<div class="mainContent">
    <div class="title"><h1>{{ 'exam_longtime'|trans({}, 'exam') }}</h1></div>
    {{ settings['training_exam_long_time']|raw }}
    <div class="links">
        <a class="bg-red" href="{{ path('my_training', { id: subject.id }) }}">{{ 'exam_list'|trans({}, 'exam') }}</a>
    </div>
</div>
{% endblock content %}
