<style>
    .payment form input {
        border: solid 1px black;
    }
    .text-error {
        color: red;
    }
</style>

<div class="container">
<div class="wrap">
    <div class="payment">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <div class="go"><span class="btn2">{{ 'registration.submit'|trans({}, 'FOSUserBundle') }} <input value="" type="submit"></span></div>
        {{ form_end(form) }}
    </div>
</div>
</div>
